<section class="content-section">
  <h1>Music</h1>

  <div id="music">
  {{#each music}}
    <div id="{{ artist_id }}" class="cover">
      <h4>{{ artist_name }}</h4>
      <img src="images/{{ artist_photo }}" alt="{{ artist_name }}">
    </div>
  {{/each}}
  </div>

</section>
<div class="lightbox">

</div>
